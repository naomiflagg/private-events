<%= form_with model: @event do |f| %>
  <% if @event.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(@event.errors.count, 'error') %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name, value: 'Event name' %>
  <%= f.text_field :name, placeholder: 'Awesome party' %>
  <%= f.label :date %>
  <%= f.date_field :date, value: 'Event date', min: Date.today %>
  <%= f.label :location %>
  <%= f.text_field :location, placeholder: 'The Warehouse' %>
  <%= f.submit %>
<% end %>